---

- name: Init a new swarm with default parameters
  docker_swarm:
    state: present
    tls: yes
  register: ready_swarm_manager
- name: Export Swarm Workers token
  set_fact:
    swarm_worker_token: "{{ ready_swarm_manager.JoinTokens.Worker }}"

# - debug: var=ansible_facts