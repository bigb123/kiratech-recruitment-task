---

- name: Init a new swarm with default parameters
  docker_swarm:
    state: present
    tls: yes
  register: docker_swarm_manager

- debug: var=ansible_facts
# - name: docker_swarm_manager value
#   debug:
#     msg: "{{ docker_swarm_manager }}"